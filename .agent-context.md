# xBridge MCP — Agent Context (Shared)

This file is the single shared context for all agents and subagents working on this project.

## Project Identity
xBridge MCP: MCP server exposing xAI Grok API as 19+ tools. Python 3.10+, async httpx, mcp>=1.0.0.

## Core Files & Directories (always reference these paths)
- CLAUDE.md — full project spec, architecture, key patterns, deployment
- STANDARDS.md — Universal Core v2.0.0 (canonical reference)
- .workflow-prompt.md — xBridge project overlay (Python + MCP specifics only)
- xbridge_mcp/server.py — main MCP server + all tool handlers (~1960 lines)
- xbridge_mcp/session_manager.py — JSON session persistence
- xbridge_mcp/tool_chains.py — chain execution logic
- xbridge_mcp/token_counter.py — token counting utility (new reusable module)
- .claude/agents/ — existing 9+ specialized subagents
- site/ — product website (xbridgemcp.com)
- xbrdg-site/ — token landing page
- tests/ — pytest suite (mocked httpx)
- scripts/deploy.sh — deployment script

## Current Standards (references STANDARDS.md Core v2.0.0)
- Git: tiered branching + worktrees for parallel work
- Code: self-explanatory, modular, composition over inheritance, reliable
- Endpoints: always label HUMAN / AGENT / INTERNAL
- Subagents: explicit paths + .agent-context.d/ fragments; main agent consolidates
- In-flight state: `.agent-context.md` must contain "Current in-flight task / next step" section
- New utilities: always document in LIBRARY.md

## Key Rules for All Agents
- All xAI calls go through make_grok_request()
- Async everywhere
- Never hardcode XAI_API_KEY
- MCP tools are AGENT surface

## Current In-Flight Task / Next Step
None active. Last session ended after STANDARDS.md v2.0.0 hardening.

Last updated: 2026-06-13 by main agent (aligned to Core v2.0.0)