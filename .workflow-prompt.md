# xBridge MCP — Project Overlay

This file contains only xBridge-specific rules that extend the Universal Core (STANDARDS.md v2.0.0).

## References
- Universal Core: STANDARDS.md v2.0.0 (~/mylab/forge or vendored)
- Full project spec: CLAUDE.md

## xBridge-Specific Rules
- Language: Python 3.10+ with full type hints and async (httpx)
- MCP surface: All tools registered via `@server.list_tools()` / `@server.call_tool()` are AGENT endpoints
- Key patterns (from CLAUDE.md):
  - All xAI calls go through `make_grok_request()`
  - Sessions stored as JSON in `.grok_sessions/`
  - Never hardcode `XAI_API_KEY`

## Local Paths
See .agent-context.md for the authoritative list of files and directories.

This overlay must never contradict the Universal Core.