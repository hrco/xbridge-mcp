AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31

Globals:
  Function:
    Runtime: python3.12
    Timeout: 10
    Environment:
      Variables:
        TABLE_NAME: !Ref KeysTable
        SES_FROM: !Ref SesFrom
        LS_SIGNING_SECRET: !Ref LsSigningSecret

Parameters:
  TableName:
    Type: String
    Default: xbridge_keys
  SesFrom:
    Type: String
    Default: hello@xbridgemcp.com
  LsSigningSecret:
    Type: String
    NoEcho: true
  LsApiKey:
    Type: String
    NoEcho: true
  LsStoreId:
    Type: String

Resources:
  KeysTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Ref TableName
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: key
          AttributeType: S
        - AttributeName: email
          AttributeType: S
        - AttributeName: subscription_id
          AttributeType: S
      KeySchema:
        - AttributeName: key
          KeyType: HASH
      GlobalSecondaryIndexes:
        - IndexName: email-index
          KeySchema:
            - AttributeName: email
              KeyType: HASH
          Projection:
            ProjectionType: ALL
        - IndexName: subscription-index
          KeySchema:
            - AttributeName: subscription_id
              KeyType: HASH
          Projection:
            ProjectionType: KEYS_ONLY
      TimeToLiveSpecification:
        AttributeName: TTL
        Enabled: true

  ValidateKeyFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: validate_key.handler.lambda_handler
      CodeUri: src/
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref KeysTable
      Events:
        Api:
          Type: HttpApi
          Properties:
            Path: /validate
            Method: post

  CreateFreeKeyFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: create_free_key.handler.lambda_handler
      CodeUri: src/
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref KeysTable
        - SESCrudPolicy:
            IdentityName: !Ref SesFrom
        - Statement:
            - Effect: Allow
              Action: ses:SendEmail
              Resource: '*'
      Events:
        Api:
          Type: HttpApi
          Properties:
            Path: /keys/free
            Method: post

  WebhookHandlerFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: webhook_handler.handler.lambda_handler
      CodeUri: src/
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref KeysTable
        - SESCrudPolicy:
            IdentityName: !Ref SesFrom
        - Statement:
            - Effect: Allow
              Action: ses:SendEmail
              Resource: '*'
      Events:
        Api:
          Type: HttpApi
          Properties:
            Path: /webhooks/ls
            Method: post

  ResendKeyFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: resend_key.handler.lambda_handler
      CodeUri: src/
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref KeysTable
        - SESCrudPolicy:
            IdentityName: !Ref SesFrom
        - Statement:
            - Effect: Allow
              Action: ses:SendEmail
              Resource: '*'
      Events:
        Api:
          Type: HttpApi
          Properties:
            Path: /keys/resend
            Method: post

  KeyUsageFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: key_usage.handler.lambda_handler
      CodeUri: src/
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref KeysTable
      Events:
        Api:
          Type: HttpApi
          Properties:
            Path: /keys/usage
            Method: post

  XbrdgDiscountFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: xbrdg_discount.handler.lambda_handler
      CodeUri: src/
      Policies: []
      Events:
        Api:
          Type: HttpApi
          Properties:
            Path: /keys/verify-xbrdg
            Method: post
      Environment:
        Variables:
          LS_API_KEY: !Ref LsApiKey
          LS_STORE_ID: !Ref LsStoreId

Outputs:
  ApiUrl:
    Value: !Sub "https://${ServerlessHttpApi}.execute-api.${AWS::Region}.amazonaws.com"
